# Config with secrets
data/config.yml
data/*.db*
data/backups/
data/eval/
*.bak

# Environment variables (may contain API keys)
.env
.env.*
!.env.example

# Python
__pycache__/
*.py[cod]
*$py.class
*.egg-info/
dist/
build/

# Virtual environment
.venv/
venv/

# IDE
.vscode/
.idea/
*.swp
*.swo

# OS
.DS_Store
Thumbs.db

# Claude / AI tooling
.claude/
.claude-runner/

# Build / lint caches
.mypy_cache/
.pytest_cache/
.ruff_cache/
.coverage
htmlcov/

# extra
personal-docs/
.gstack/

# Private development docs (public docs live in repo root)
docs/
.superpowers

# uv lockfile: artifact of the local uv runner; deps are locked via pip-compile
# (requirements*.txt). Ignore so `uv run` doesn't add a redundant, drifting lock.
uv.lock
